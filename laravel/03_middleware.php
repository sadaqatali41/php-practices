<?php

// * Middleware provide a convenient mechanism for inspecting and filtering HTTP requests entering your application. It is acts as a bridge between request and response.

// * it can be used for logging for all incoming request.

// * before and after middleware

// * php artisan make:middleware LoggingMiddleware